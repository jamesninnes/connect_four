import 'package:flutter/material.dart';

class vsAI extends StatefulWidget {
  vsAI({Key key}) : super(key: key);

  _vsAIState createState() => _vsAIState();
}

class _vsAIState extends State<vsAI> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('vs AI'),),
       body: Center(child: Text('AI not implemented yet'),),
    );
  }
}